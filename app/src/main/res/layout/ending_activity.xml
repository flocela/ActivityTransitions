<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
              android:id="@+id/root_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                tools:context=".MainActivity"
                android:orientation="vertical">
  <ImageView
    android:id="@+id/top_dot"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:src="@drawable/circle_24dp"
    android:tint="@color/indigo_primary"
    android:transitionName="dot_transition"
    android:layout_gravity="right"
    android:paddingRight="20dp"/>
  <TextView
    android:id="@+id/more_info_from_bottom"
    android:text="The Start From The Bottom. We dont like to do too much explaining
Story stay the same through the money and the fame. Cause we... Drake - MetroLyrics "
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:textSize="30sp"/>
</LinearLayout>
